class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        # we wil need to concatenate two lists to make a better.
        # integer array nums of length
        return nums+nums

        # In this we need to append the same array to itself now we will try to append the array
        